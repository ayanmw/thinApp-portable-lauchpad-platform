# MVP 发布前检查清单

**项目**: AI-ThinApp Portable Launchpad Platform  
**版本**: MVP (v1.0.0)  
**日期**: 2026-05-24  
**负责人**: 产品经理  

---

## 📋 检查清单

| # | 检查项 | 负责角色 | 状态 | 验证方法 | 备注 |
|---|--------|----------|------|----------|------|
| 1 | 编译验证通过（所有模块） | 开发工程师 | ⏳ 待完成 | 运行 `.\tools\build\build_hook_simple.bat`，确认 0 错误 0 警告 | 阻塞项 |
| 2 | 单元测试通过率 ≥ 80% | 测试工程师 | ⏳ 待完成 | 运行 `tools/ci/run_coverage.bat`，查看覆盖率报告 | 目标 ≥ 80% |
| 3 | 集成测试通过 | 测试工程师 | ⏳ 待完成 | 运行 `tools/ci/run_integration_tests.bat`，所有测试用例通过 | 阻塞项 |
| 4 | 性能基准测试通过（启动 < 3s、内存 < 200MB） | 性能工程师 | ⏳ 待完成 | 运行 `tools/ci/run_perf_tests.bat`，验证启动时间和内存占用 | 阻塞项 |
| 5 | 代码签名验证通过（EV 证书） | 代码签名工程师 | ⏳ 待完成 | 插入 USB Token，运行 `tools/ci/verify_signature.ps1`，确认签名有效 | 阻塞项 |
| 6 | 应用商店材料完整（图标、截图、描述） | 产品经理 | ✅ 已完成 | 检查 `docs/store-assets/` 目录，确认所有材料齐全 | 已完成 |
| 7 | 用户文档完整（手册、FAQ、教程） | 技术文档工程师 | ✅ 已完成 | 检查 `docs/user-guide/` 目录，确认所有文档齐全 | 已完成 |
| 8 | 部署文档完整（安装包、签名、商店上架） | 部署工程师 | ✅ 已完成 | 检查 `docs/deployment/` 目录，确认所有文档齐全 | 已完成 |
| 9 | 已知问题清单已整理 | 产品经理 | ⏳ 待完成 | 创建 `docs/MVP-KNOWN-ISSUES.md`，列出所有已知问题 | 进行中 |
| 10 | 后续路线图已规划（MVP+1 阶段） | 产品经理 | ⏳ 待完成 | 创建 `docs/MVP-ROADMAP.md`，规划 MVP+1 阶段目标 | 进行中 |

---

## 🚦 状态说明

| 状态 | 说明 |
|------|------|
| ✅ 已完成 | 检查项已通过验证 |
| ⏳ 待完成 | 检查项尚未完成，需要执行 |
| ⚠️ 有风险 | 检查项已完成，但存在风险或问题 |
| ❌ 未通过 | 检查项未通过验证，需要修复 |

---

## 🔴 阻塞项

以下检查项为 MVP 发布的阻塞项，必须在发布前通过：

1. **编译验证通过** - 所有模块必须编译通过，0 错误 0 警告
2. **集成测试通过** - 所有集成测试用例必须通过
3. **性能基准测试通过** - 启动时间 < 3s，内存占用 < 200MB
4. **代码签名验证通过** - EV 代码签名必须有效

---

## ✅ 已完成项

以下检查项已完成，无需再次验证：

1. **应用商店材料完整** - 图标、截图、描述已准备齐全
2. **用户文档完整** - 用户手册、FAQ、教程已准备齐全
3. **部署文档完整** - 安装包、签名、商店上架文档已准备齐全

---

## 📝 验证方法

### 1. 编译验证

```bash
# 运行编译脚本
.\tools\build\build_hook_simple.bat

# 验证输出
# - 0 错误
# - 0 警告
# - 所有模块编译成功
```

### 2. 单元测试

```bash
# 运行全模块测试
tools\ci\run_all_tests.bat

# 运行覆盖率测试
tools\ci\run_coverage.bat

# 验证覆盖率报告
# - 目标：≥ 80%
# - 关键模块：≥ 90%
```

### 3. 集成测试

```bash
# 运行集成测试
tools\ci\run_integration_tests.bat

# 验证测试结果
# - 所有测试用例通过
# - 无阻塞性问题
```

### 4. 性能基准测试

```bash
# 运行性能测试
tools\ci\run_perf_tests.bat

# 验证性能指标
# - 启动时间 < 3s
# - 内存占用 < 200MB
# - Hook 开销 < 10%
```

### 5. 代码签名验证

```bash
# 插入 EV 代码签名 USB Token

# 运行签名脚本
tools\ci\sign_ev.ps1

# 添加时间戳
tools\ci\sign_timestamp.ps1

# 验证签名
tools\ci\verify_signature.ps1

# 验证结果
# - 签名有效
# - 证书链完整
# - 时间戳有效
```

---

## 📊 进度跟踪

- **总检查项**: 10
- **已完成**: 3
- **待完成**: 5
- **阻塞项**: 4
- **完成度**: 30%

---

## 🎯 下一步行动

1. **等待编译完成** - 等待 `mvp-fix-all-bat-files` 和 `mvp-fix-vreg-actual` 任务完成
2. **执行测试验证** - 编译通过后，立即执行所有测试
3. **完成已知问题清单** - 整理 POC 阶段未解决的问题
4. **完成后续路线图** - 规划 MVP+1 阶段目标
5. **准备发布流程** - 确保编译通过后 1 小时内可启动发布流程

---

## 📄 相关文档

- [MVP 发布流程](./MVP-LAUNCH-PROCESS.md)
- [已知问题清单](./MVP-KNOWN-ISSUES.md)
- [后续路线图](./MVP-ROADMAP.md)
- [发布准备报告](./MVP-LAUNCH-PREP-REPORT.md)

---

**最后更新**: 2026-05-24  
**更新人**: 产品经理
