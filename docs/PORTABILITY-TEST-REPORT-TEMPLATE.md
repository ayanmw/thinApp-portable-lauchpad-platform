# 便携性验证报告

> **文档说明**：本报告模板用于记录每次便携性验证的结果。每次测试后，复制此模板填写结果，保存为 `PORTABILITY-TEST-REPORT-{应用名称}-{版本}-{日期}.md`。

---

## 1. 测试基本信息

- **应用名称**：（填写应用名称，如：Notepad++）
- **应用版本**：（填写应用版本，如：v8.6.4）
- **测试日期**：（填写测试日期，如：2026-05-24）
- **测试人员**：（填写测试人员姓名）
- **测试环境**：
  - **本机**：
    - 操作系统：（如：Windows 10 22H2，Build 19045）
    - CPU：（如：Intel Core i7-12700H）
    - 内存：（如：16GB DDR4）
    - 磁盘：（如：512GB NVMe SSD）
  - **虚拟机 A**（若使用）：
    - 操作系统：（如：Windows 10 22H2，Build 19045）
    - 虚拟化软件：（如：VMware Workstation 17 Pro）
    - CPU 分配：（如：2 vCPU）
    - 内存分配：（如：4GB）
    - 磁盘分配：（如：60GB）
  - **虚拟机 B**（若使用）：
    - 操作系统：（如：Windows 11 23H2，Build 22631）
    - 虚拟化软件：（如：VMware Workstation 17 Pro）
    - CPU 分配：（如：2 vCPU）
    - 内存分配：（如：4GB）
    - 磁盘分配：（如：60GB）
  - **目标机器**（若跨机器测试）：
    - 操作系统：（如：Windows 10 22H2，Build 19045）
    - CPU：（如：Intel Core i5-10400F）
    - 内存：（如：16GB DDR4）
    - 磁盘：（如：1TB SATA SSD）

---

## 2. 测试结果汇总

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|----------|----------|----------|------|
| PT-01 本机不同路径 | 应用启动，文件重定向到新路径 | （填写实际结果） | ✅/❌ |
| PT-02 跨机器（Win10 → Win10） | 应用启动，功能正常 | （填写实际结果） | ✅/❌ |
| PT-03 跨机器（Win10 → Win11） | 应用启动，功能正常 | （填写实际结果） | ✅/❌ |
| PT-04 U 盘移动场景 | 应用启动，功能正常 | （填写实际结果） | ✅/❌ |
| PT-05 用户配置迁移 | 配置已迁移 | （填写实际结果） | ✅/❌ |
| PT-06 插件/扩展迁移 | 插件可用 | （填写实际结果） | ✅/❌ |
| PT-07 大文件处理 | 文件正确保存在 VFS | （填写实际结果） | ✅/❌ |
| PT-08 注册表重定向 | 注册表数据来自虚拟 hive | （填写实际结果） | ✅/❌ |

**通过率**：X/8

---

## 3. 详细结果

### PT-01 本机不同路径

#### 3.1.1 操作步骤

1. 复制 `D:\MyApps\Notepad++\` 到 `E:\PortableApps\Notepad++\`
2. 双击 `E:\PortableApps\Notepad++\Notepad++.exe`
3. 验证：
   - 应用正常启动
   - 打开文件对话框，默认路径是 `E:\PortableApps\Notepad++\` 下的 VFS 目录
   - 保存文件，文件出现在 `E:\PortableApps\Notepad++\VFS\...`
   - 查看注册表，所有读取都来自 `E:\PortableApps\Notepad++\VFS\registry.hive`

#### 3.1.2 预期结果

- 应用正常启动
- 所有文件操作重定向到新路径
- 注册表所有操作重定向到新路径
- 相对路径解析正确（插件路径、配置文件路径）

#### 3.1.3 实际结果

（填写实际结果，包括：
- 应用是否启动成功
- 文件重定向是否正确（检查 VFS 目录）
- 注册表重定向是否正确（检查 registry.hive）
- 是否有错误日志）

#### 3.1.4 日志文件

（若有错误，附上日志路径，如：
- `{AppDir}\logs\hook-engine.log`
- `{AppDir}\logs\vfs.log`
- `{AppDir}\logs\vreg.log`）

#### 3.1.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-02 跨机器（Win10 → Win10）

#### 3.2.1 操作步骤

1. 将 `D:\MyApps\Notepad++\` 复制到虚拟机 A 的 `C:\PortableApps\Notepad++\`
2. 双击 `C:\PortableApps\Notepad++\Notepad++.exe`
3. 验证：
   - 应用正常启动（虚拟机 A 没有安装 Notepad++）
   - 所有功能正常（打开文件、保存文件、插件加载）
   - 关闭应用后，`C:\PortableApps\Notepad++\VFS\` 中出现运行时生成的文件

#### 3.2.2 预期结果

- 应用正常启动（干净系统，未安装）
- 所有功能正常
- 运行时文件生成在 VFS
- 真实系统未受污染

#### 3.2.3 实际结果

（填写实际结果）

#### 3.2.4 日志文件

（若有错误，附上日志路径）

#### 3.2.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-03 跨机器（Win10 → Win11）

#### 3.3.1 操作步骤

1. 将 `D:\MyApps\Notepad++\` 复制到虚拟机 B 的 `C:\PortableApps\Notepad++\`
2. 双击 `C:\PortableApps\Notepad++\Notepad++.exe`
3. 验证应用功能正常

#### 3.3.2 预期结果

- 应用正常启动
- 所有功能正常（Win11 兼容性）

#### 3.3.3 实际结果

（填写实际结果）

#### 3.3.4 日志文件

（若有错误，附上日志路径）

#### 3.3.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-04 U 盘移动场景

#### 3.4.1 操作步骤

1. 将 `D:\MyApps\Notepad++\` 复制到 U 盘 `F:\PortableApps\Notepad++\`
2. 在另一台电脑（非虚拟机）上，双击 `F:\PortableApps\Notepad++\Notepad++.exe`
3. 验证应用功能正常

#### 3.4.2 预期结果

- 应用正常启动
- 文件读写速度正常（无明显卡顿）
- 安全删除后无残留

#### 3.4.3 实际结果

（填写实际结果）

#### 3.4.4 日志文件

（若有错误，附上日志路径）

#### 3.4.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-05 用户配置迁移

#### 3.5.1 操作步骤

1. 在"本机"启动 Notepad++，修改设置（主题、字体、语言）
2. 关闭应用
3. 复制应用目录到虚拟机 A
4. 在虚拟机 A 启动 Notepad++，验证设置已迁移

#### 3.5.2 预期结果

- 用户配置已迁移（主题、字体、语言）
- 配置文件存储在 `VFS\AppData\Roaming\Notepad++\`

#### 3.5.3 实际结果

（填写实际结果）

#### 3.5.4 日志文件

（若有错误，附上日志路径）

#### 3.5.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-06 插件/扩展迁移

#### 3.6.1 操作步骤

1. 在"本机"启动 Notepad++，安装插件（NppExport、Mime Tools）
2. 关闭应用
3. 复制应用目录到虚拟机 A
4. 在虚拟机 A 启动 Notepad++，验证插件可用

#### 3.6.2 预期结果

- 插件文件存储在 `VFS\C\Program Files\Notepad++\plugins\`
- 插件在目标机器上可用

#### 3.6.3 实际结果

（填写实际结果）

#### 3.6.4 日志文件

（若有错误，附上日志路径）

#### 3.6.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-07 大文件处理

#### 3.7.1 操作步骤

1. 在"本机"启动 Notepad++，打开 100MB 文件
2. 编辑后保存
3. 检查文件保存在 VFS
4. 复制应用目录到虚拟机 A
5. 在虚拟机 A 启动 Notepad++，验证大文件可打开

#### 3.7.2 预期结果

- 打开 100MB 文件正常（无内存溢出）
- 保存文件到 VFS 正常
- 复制目录后，大文件仍在 VFS 中

#### 3.7.3 实际结果

（填写实际结果）

#### 3.7.4 日志文件

（若有错误，附上日志路径）

#### 3.7.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

### PT-08 注册表重定向

#### 3.8.1 操作步骤

1. 在"本机"启动 Notepad++, 修改设置（写入注册表）
2. 关闭应用
3. 使用注册表工具检查 `{AppDir}\VFS\registry.hive`
4. 复制应用目录到虚拟机 A
5. 在虚拟机 A 启动 Notepad++，验证注册表数据来自虚拟 hive

#### 3.8.2 预期结果

- 注册表读取来自虚拟 hive
- 注册表写入到虚拟 hive
- 真实注册表未受影响

#### 3.8.3 实际结果

（填写实际结果）

#### 3.8.4 日志文件

（若有错误，附上日志路径）

#### 3.8.5 结论

✅ 通过 / ❌ 失败

（若失败，填写失败原因和解决方案）

---

## 4. 问题与建议

### 4.1 问题记录

#### 问题 1：（填写问题标题）

- **描述**：（填写问题描述）
- **严重程度**：致命/高/中/低
- **影响范围**：（填写影响范围，如：所有应用 / 仅 Notepad++）
- **复现步骤**：
  1. （填写步骤 1）
  2. （填写步骤 2）
  3. （填写步骤 3）
- **解决方案**：（若有，填写解决方案；若无，填写"待分析"）
- **状态**：待解决/解决中/已解决
- **解决日期**：（填写解决日期）

#### 问题 2：（若有更多问题，继续添加）

- **描述**：
- **严重程度**：
- **影响范围**：
- **复现步骤**：
- **解决方案**：
- **状态**：
- **解决日期**：

---

### 4.2 改进建议

#### 建议 1：（填写建议标题）

- **描述**：（填写建议描述）
- **理由**：（填写建议理由）
- **优先级**：高/中/低
- **提出日期**：（填写提出日期）
- **状态**：待评估/已采纳/已拒绝

#### 建议 2：（若有更多建议，继续添加）

- **描述**：
- **理由**：
- **优先级**：
- **提出日期**：
- **状态**：

---

## 5. 结论

### 5.1 总体评价

（填写总体评价，包括：
- 应用的便携性表现
- 优点
- 缺点
- 改进方向）

### 5.2 Go / No-Go 决策

- [ ] **应用满足便携性要求**（所有测试用例通过）
  - 可以进入 MVP 阶段
  - 建议：无

- [ ] **应用基本满足便携性要求**（部分测试用例失败，但不影响核心功能）
  - 可以有条件进入 MVP 阶段
  - 建议：修复失败项，纳入 MVP 任务清单

- [ ] **应用不满足便携性要求**（核心功能失败）
  - 不可以进入 MVP 阶段
  - 建议：重新评估技术路线（如切换到内核驱动方案）

### 5.3 后续行动计划

（填写后续行动计划，包括：
- 修复问题的计划
- 优化功能的计划
- 下一步测试计划）

---

## 6. 附件

### 6.1 日志文件

- 日志文件：`{AppDir}\logs\`
  - `hook-engine.log`：（Hook 引擎日志）
  - `vfs.log`：（虚拟文件系统日志）
  - `vreg.log`：（虚拟注册表日志）
  - `launcher.log`：（启动器日志）

### 6.2 截图

- 截图：`{AppDir}\screenshots\`
  - `pt01-file-dialog.png`：（PT-01 文件对话框截图）
  - `pt01-save-file.png`：（PT-01 保存文件截图）
  - `pt02-app-launch.png`：（PT-02 应用启动截图）
  - ...（更多截图）

### 6.3 测试脚本输出

- 测试脚本输出：`tools\ci\test-portability-report.html`
  - （自动化测试生成的 HTML 报告）

### 6.4 其他附件

（若有 other 附件，继续添加）

---

## 7. 审批记录

| 角色 | 姓名 | 审批意见 | 审批日期 |
|------|------|----------|----------|
| 测试负责人 |  |  |  |
| 开发负责人 |  |  |  |
| PM |  |  |  |

---

## 8. 修订历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 0.1 | （填写日期） | （填写作者） | 初版，基于测试结果输出 |

---

**文档结束**

---

## 附录：填写示例

### 示例 1：PT-01 实际结果（通过）

```
应用成功启动。
文件重定向正确：打开文件对话框默认路径为 `E:\PortableApps\Notepad++\VFS\AppData\Roaming\Notepad++\`，保存的文件出现在 `E:\PortableApps\Notepad++\VFS\...`。
注册表重定向正确：使用 RegEdit 加载 `E:\PortableApps\Notepad++\VFS\registry.hive`，确认设置存储在虚拟 hive 中。
相对路径解析正确：插件从 `E:\PortableApps\Notepad++\VFS\C\Program Files\Notepad++\plugins\` 加载，功能正常。
无错误日志。
```

### 示例 2：PT-02 实际结果（失败）

```
应用启动失败，错误提示："无法找到模块 VFS.dll"。
检查日志文件 `{AppDir}\logs\launcher.log`，发现错误："LoadLibrary failed: VFS.dll not found"。
可能原因：应用目录复制不完整，缺少 VFS.dll。
解决方案：重新复制应用目录，确认所有文件都已复制。
```

### 示例 3：问题记录（填写示例）

```
问题 1：应用启动慢
- 描述：在虚拟机 A 上，应用启动时间超过 10 秒（本机启动时间 < 2 秒）
- 严重程度：中
- 影响范围：所有应用
- 复现步骤：
  1. 复制应用目录到虚拟机 A
  2. 双击 exe
  3. 计时启动时间
- 解决方案：待分析。可能原因：虚拟机磁盘 IO 性能差；VFS 初始化慢。
- 状态：待解决
- 解决日期：
```
